#!/usr/bin/env bash
# Global commit-msg hook: strip AI-attribution trailers from the final message.
# Installed by ns-bootstrap — applies to all repositories.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
. "$SCRIPT_DIR/lib/strip-ai-trailers.sh"
strip_ai_trailers "$1"
