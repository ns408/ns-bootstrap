# Security hardening — prevent malicious lifecycle scripts
ignore-scripts=true

# Audit settings
audit-level=moderate

# Disable funding messages
fund=false

# Use exact versions by default
save-exact=true
